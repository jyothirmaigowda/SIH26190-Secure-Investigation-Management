// The final SIMS data model will be agreed by the team before models are added.
// Keep DATABASE_URL in an ignored .env file; never commit credentials or real data.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
